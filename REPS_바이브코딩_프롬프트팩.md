# REPS — 바이브 코딩 프롬프트 팩

주식 트레이딩 훈련 서비스를 AI 코딩 도구로 단계별 구현하기 위한 프롬프트 모음입니다.

**사용 도구:** Claude Code / Cursor / Windsurf / Lovable 등
**총 단계:** 7단계 (각 단계 완료 후 다음 단계로)
**예상 소요:** 단계당 20~60분

---

## 사용법

1. **`[MASTER]` 프롬프트를 먼저 붙여넣습니다.** 이건 프로젝트 헌법입니다. 새 세션을 시작할 때마다 다시 붙여넣으세요.
2. `[STAGE 1]`부터 순서대로 하나씩 붙여넣습니다. **여러 단계를 한 번에 주지 마세요** — AI가 반드시 대충 만듭니다.
3. 각 단계 끝의 **완료 기준**을 직접 눈으로 확인한 뒤 다음으로 넘어갑니다.
4. 이상하면 `[QA]` 프롬프트를 붙여넣어 자가 점검시킵니다.

> 단계를 건너뛰면 3단계(결과 잠금)가 반드시 깨집니다. 그게 이 제품의 전부이므로 순서를 지키세요.

---

# [MASTER] 프로젝트 헌법

```
당신은 "REPS"라는 웹 서비스를 만듭니다. 아래 내용을 프로젝트의 절대 원칙으로 삼고,
이후 모든 요청에서 이 원칙을 위반하지 마세요. 원칙과 충돌하는 요청이 오면 먼저 지적하세요.

## 이 제품이 무엇인가

주식 트레이딩을 "연습"하고 그 연습을 "채점"해서, 실전에 나갈 준비가 됐는지
시스템이 판정해주는 훈련 도구입니다.

실제 주문을 내지 않습니다. 실제 시세를 쓰지 않습니다. 종목을 추천하지 않습니다.
투자 자문이 아닙니다. 전부 연습용 가상 데이터입니다.

## 대상 사용자

주식 투자를 잘 모르는 사람입니다. "손절", "지지선", "기대값" 같은 말을 모른다고 가정하세요.
전문 용어를 쓸 때는 반드시 그 자리에서 뜻을 알려줘야 합니다.

## 절대 원칙 5개

1. **결과를 먼저 보여주지 않는다.**
   사용자가 자기 판단을 채점하기 전까지, 손익·수익률·결과 차트를 어떤 형태로도
   노출하지 않습니다. 화면에서 가리는 게 아니라 데이터 자체를 넘기지 않습니다.

2. **입력은 30초 안에 끝난다.**
   기록에 30초 넘게 걸리면 사용자는 반드시 그만둡니다.
   필드를 추가하려면 기존 필드를 빼세요. 제로섬입니다.

3. **모든 성과는 'R' 단위로 잰다.**
   금액이 아니라 R(손절폭 = 1R)로 계산합니다. 이래야 서로 다른 종목·금액의
   연습이 하나의 통계로 합쳐집니다.

4. **피드백은 즉시 준다.**
   연습 1회가 끝나면 3초 안에 화면에 결과가 나와야 합니다.
   로딩 스피너를 돌리지 말고, 계산은 전부 클라이언트에서 끝내세요.

5. **수익률로 순위를 매기지 않는다.**
   리더보드, 수익률 랭킹, 수익 자랑 기능을 절대 만들지 마세요.
   운 좋은 사람이 1등이 되는 순간 제품 철학이 무너집니다.

## 기술 스택 (변경 금지)

- Next.js 15 (App Router) + TypeScript (strict) + Tailwind CSS
- shadcn/ui 컴포넌트
- 차트: lightweight-charts (TradingView 오픈소스 라이브러리)
- 상태: Zustand
- 저장: 1~5단계는 localStorage, 6단계에서 Supabase로 이전
- 테스트: Vitest (계산 로직만)
- (2026년 추가) AI 코치 조언(선택 기능): Anthropic Claude API. 아래 "출시 이후 추가된 기능" 참고

## 디자인 원칙

전문적인 계측 장비 느낌을 목표로 합니다. SaaS 랜딩페이지 느낌을 내지 마세요.

- 배경은 흰색(#FFFFFF), 차트 패널만 짙은 남색으로 대비를 줍니다
- 숫자는 반드시 등폭 폰트 + tabular-nums (자릿수가 흔들리면 안 됨)
- 그라디언트, 글래스모피즘, 보라색 계열, 이모지 아이콘 금지
- 애니메이션은 150ms 이하, 데이터가 바뀔 때만
- 여백을 아끼지 마세요. 정보 밀도는 높되 답답하지 않게

### 색상 토큰 (globals.css에 CSS 변수로 정의)

--ink:      #0F1D2E   /* 주요 텍스트, 짙은 패널 */
--ink-2:    #1E3552   /* 보조 짙은 면 */
--paper:    #FFFFFF   /* 배경 */
--surface:  #F4F6F9   /* 카드 */
--border:   #E2E8F0
--muted:    #64748B   /* 보조 텍스트 */
--up:       #D93025   /* 상승 = 빨강 (한국 증시 관례) */
--down:     #1265CC   /* 하락 = 파랑 (한국 증시 관례) */
--good:     #1B7F79   /* 잘한 판단 */
--warn:     #E4572E   /* 경고, 잠금 */

주의: 미국식(상승=초록/하락=빨강)을 쓰지 마세요. 한국 사용자 대상입니다.

### 폰트

- 한글/본문: Pretendard (CDN)
- 숫자/데이터: JetBrains Mono, font-variant-numeric: tabular-nums
- 제목 36~40px bold / 섹션 20px bold / 본문 14~15px / 캡션 12px

## 코드 규칙

- 계산 로직(R, 기대값, 필요 표본 수, 등급)은 전부 `lib/` 아래 순수 함수로 분리하고
  각각 Vitest 테스트를 함께 작성합니다. UI 컴포넌트 안에서 계산하지 마세요.
- 모든 사용자 대면 문구는 한국어입니다.
- 전문 용어를 화면에 쓸 때는 반드시 <Term> 컴포넌트로 감싸 툴팁이 뜨게 합니다.

준비됐으면 "확인"이라고만 답하고 기다리세요. 아직 코드를 쓰지 마세요.
```

---

# [STAGE 1] 골격 + 디자인 시스템 + 용어 사전

```
1단계를 구현하세요. 아직 연습 기능은 만들지 않습니다.

## 만들 것

### 1. 프로젝트 셋업
Next.js 15 + TypeScript + Tailwind + shadcn/ui 초기화.
globals.css에 MASTER의 색상 토큰과 폰트를 CSS 변수로 정의.

### 2. 레이아웃
- 상단 고정 바: 왼쪽에 로고 "REPS", 오른쪽에 현재 단계 뱃지(예: "1단계 · 실행")
  와 진척 표시("이번 주 12회")
- 본문은 최대 폭 1280px 중앙 정렬
- 모바일에서는 하단 탭바 (연습 / 기록 / 진척)

### 3. <Term> 컴포넌트 — 이게 이 단계의 핵심
전문 용어를 감싸면 점선 밑줄이 생기고, 마우스 오버(모바일은 탭)하면
쉬운 설명이 뜹니다.

사용 예: <Term id="son-jeol">손절</Term>

용어 데이터는 `lib/glossary.ts`에 아래 내용으로 만드세요:

  son-jeol      | 손절       | 내 예상이 틀렸다고 인정하고 파는 것. 미리 정해둔 가격에 자동으로 팝니다.
  r-multiple    | R          | 손절했을 때 잃는 금액을 '1'로 놓고 재는 단위. +2R은 손절폭의 2배를 벌었다는 뜻입니다.
  gi-dae-gap    | 기대값     | 한 번 거래할 때 평균적으로 몇 R을 버는지. +0.2R이면 100번 하면 평균 20R을 법니다.
  seung-ryul    | 승률       | 이긴 횟수의 비율. 중요해 보이지만 함정이 있어서 이 서비스는 크게 안 보여줍니다.
  set-up        | 셋업       | "이런 모양이 나오면 산다"는 나만의 규칙. 매번 다르게 사면 실력이 안 늘어서 필요합니다.
  jin-ip        | 진입       | 주식을 사는 것.
  cheong-san    | 청산       | 갖고 있던 주식을 파는 것. 이익이든 손실이든 상관없이 파는 행위 자체를 말합니다.
  po-ji-syeon   | 포지션     | 지금 갖고 있는 주식. "포지션을 잡는다" = 산다.
  pyo-bon       | 표본       | 지금까지 모은 연습 횟수. 적으면 잘한 건지 운인지 구별이 안 됩니다.
  jun-su-yul    | 준수율     | 미리 세운 계획을 그대로 지킨 비율. 이게 이 서비스의 1번 성적표입니다.
  ho-ga         | 호가       | 사겠다는 가격과 팔겠다는 가격의 목록.
  bong-cha-teu  | 봉차트      | 일정 시간 동안의 가격 움직임을 막대 하나로 표현한 차트. 빨간색은 오른 날, 파란색은 내린 날입니다.
  i-pyeong-seon | 이동평균선  | 최근 며칠간 평균 가격을 이은 선. 20일선은 최근 20일 평균입니다.
  nul-lim-mok   | 눌림목      | 오르던 주식이 잠깐 쉬며 내려온 자리. 여기서 다시 오를 것을 기대하고 삽니다.
  dol-pa        | 돌파       | 가격이 그동안 못 넘던 벽을 뚫고 올라가는 것.

### 4. 랜딩 화면 (/)
스크롤 없이 한 화면에 들어와야 합니다.

- 헤드라인: "연습 횟수를 성적표로 바꿉니다"
- 서브: "차트를 보고 판단하고, 결과를 보기 전에 스스로 채점합니다.
  실전에 나갈 시점은 기분이 아니라 숫자가 정합니다."
- 3개 카드로 흐름 요약: ① 미리 적기 → ② 가리고 하기 → ③ 먼저 채점
- CTA 버튼: "5분 만에 첫 연습 시작"
- 하단 고지: "실제 주문이 실행되지 않는 연습용 서비스입니다. 투자 자문이 아닙니다."

### 5. 빈 껍데기 라우트
/onboarding, /practice, /journal, /progress — 제목만 있는 빈 페이지

## 만들지 말 것
- 로그인/회원가입 (6단계까지 안 씁니다)
- 다크모드 토글
- 랜딩 페이지 애니메이션, 히어로 이미지
- 차트 관련 코드 일체

## 완료 기준
1. `npm run dev`로 실행되고 콘솔 에러가 없다
2. 랜딩 화면이 노트북 화면 하나에 스크롤 없이 들어온다
3. 아무 용어에나 마우스를 올리면 툴팁이 뜬다
4. 모바일 폭(375px)에서 레이아웃이 깨지지 않는다
```

---

# [STAGE 2] 연습 데이터 엔진 + 블라인드 차트

```
2단계를 구현하세요. 이 단계가 서비스의 물리 엔진입니다.

## 왜 가짜 데이터를 쓰는가 (중요)

실제 시세 데이터는 사용료 계약이 필요하고, 사용자가 종목·날짜를 기억하면
"미래를 아는 상태"로 연습하게 되어 훈련 효과가 사라집니다.

그래서 실제 시장과 통계적으로 비슷한 가짜 차트를 생성합니다. 이렇게 하면
정답(어떤 셋업이 심어진 구간인지)을 우리가 알고 있어서 채점이 가능해집니다.
이건 타협이 아니라 설계상의 이점입니다.

## 만들 것

### 1. `lib/market/generator.ts` — 차트 생성기

기하 브라운 운동에 레짐 전환을 얹어 일봉 OHLCV를 생성합니다.

  레짐 4종: 상승추세 / 하락추세 / 횡보 / 급등락
  레짐 전환은 마르코프 체인 (전환 확률 행렬을 상수로 노출)
  일간 변동성 1.8%, 레짐별 드리프트는 상수로 분리
  거래량은 가격 변동폭에 양의 상관을 갖게 생성

시드값을 받아 항상 같은 차트를 재생성할 수 있어야 합니다 (seedrandom 사용).

### 2. `lib/market/scenario.ts` — 시나리오 = 연습 1회분

생성한 차트에 "셋업이 성립하는 지점"을 의도적으로 심습니다.

  type Scenario = {
    id: string
    seed: number
    candles: Candle[]        // 총 180봉
    decisionIndex: number    // 120번째 봉에서 판단을 요구
    setupLabel: 'pullback' | 'breakout' | 'none'   // 정답 (사용자에게 숨김)
    regime: Regime
  }

셋업 3종을 알고리즘으로 심으세요:
- pullback(눌림목): 상승추세 중 20일선까지 3~5봉 하락 후 판단 시점
- breakout(돌파): 15봉 이상 횡보 후 상단 저항을 뚫는 봉에서 판단 시점
- none(해당 없음): 셋업 조건이 성립하지 않는 구간 — 이게 있어야
  "진입하지 않는 것"도 훈련됩니다. 전체의 30%를 none으로 만드세요.

### 3. `components/BlindChart.tsx` — 미래를 가리는 차트

lightweight-charts를 씁니다. 핵심 요구사항:

- **decisionIndex 이후의 봉은 컴포넌트에 아예 전달하지 않습니다.**
  CSS로 가리거나 오버레이를 덮는 방식은 절대 금지입니다.
  개발자 도구를 열어도 미래 데이터가 보이면 안 됩니다.
- 20일 이동평균선을 함께 그립니다
- 하단에 거래량 히스토그램
- 상승봉 빨강(--up) / 하락봉 파랑(--down)
- 종목명과 날짜를 표시하지 않습니다. "연습 #A7F2" 같은 익명 코드만 씁니다
- 차트 배경은 --ink, 그리드는 매우 옅게

`advance(n)` 메서드로 봉을 n개 진행시킬 수 있어야 합니다 (재생용).

### 4. `/practice` 화면 (기능 없이 껍데기)
- 왼쪽 70%: BlindChart
- 오른쪽 30%: 빈 사이드 패널 (3단계에서 채움)
- 상단: "이 차트를 보고 판단하세요. 오른쪽 화면은 아직 나오지 않습니다."

## 만들지 말 것
- 실제 주가 API 연동
- 분봉·틱 데이터
- 지표 추가 (20일선 하나만)
- 그리기 도구

## 완료 기준
1. /practice에 들어갈 때마다 다른 차트가 나온다
2. 같은 seed를 주면 같은 차트가 나온다
3. React DevTools로 컴포넌트 props를 열어도 미래 봉이 없다
4. 생성기 테스트: 200개 시나리오를 만들었을 때 setupLabel 분포가
   대략 pullback 35% / breakout 35% / none 30%가 나온다
```

---

# [STAGE 3] Rep Card + 결과 잠금 ★ 핵심 단계

```
3단계를 구현하세요. 이 단계가 제품의 존재 이유입니다. 대충 만들지 마세요.

## 상태 머신 (반드시 이 순서)

  WATCHING  차트를 보는 중, 아직 결정 안 함
     ↓ 진입하기 / 넘어가기
  COMMITTED 계획을 저장함. 이후 계획 수정 불가
     ↓ 차트 재생 → 청산
  EXECUTED  청산 완료. 하지만 손익은 아직 안 보임
     ↓ 판단 채점
  GRADED    자기 채점 완료
     ↓
  REVEALED  이제서야 손익 공개

`lib/rep/machine.ts`에 상태 전이 함수를 만들고, 순서를 건너뛰는 전이는
런타임에서 throw 하세요.

## 만들 것

### 1. 진입 결정 UI (WATCHING)
사이드 패널에 큰 버튼 2개:
- "여기서 산다" (--ink 배경)
- "지나간다" (테두리만)

"지나간다"도 하나의 Rep으로 기록됩니다. **아무것도 안 하는 것도 판단입니다.**
setupLabel이 'none'인데 지나갔으면 정답입니다. 이 점을 UI에 한 줄로 알려주세요.

### 2. Rep Card 입력 (COMMITTED로 가는 관문)
"여기서 산다"를 누르면 사이드 패널이 입력 폼으로 바뀝니다.

입력 필드는 딱 3개입니다. 더 늘리지 마세요.

  [1] 어떤 모양이라서 사나요?
      → 3지선다 버튼: 눌림목 / 돌파 / 기타
      → 각 선택지 아래에 한 줄 설명과 <Term> 툴팁

  [2] 얼마까지 내려가면 틀린 건가요?  (= 손절가)
      → 숫자 입력 + 아래에 실시간 안내:
        "현재가 대비 -3.2%입니다. 이만큼 내려가면 자동으로 팝니다."
        "이 금액이 당신의 1R = 32,000원입니다."
      → 현재가보다 높은 값을 넣으면 즉시 경고

  [3] 목표는 어디인가요?
      → 프리셋 버튼: 1R / 2R / 3R / 직접입력
      → 선택하면 "손절폭의 2배 = 목표가 74,000원" 실시간 표시

하단에 큰 버튼: **"계획 저장 (저장 후 수정 불가)"**
누르기 전에 확인 다이얼로그를 띄우지 마세요. 마찰이 됩니다.
대신 버튼 위에 작게 "저장하면 못 고칩니다"라고 씁니다.

**입력 시간을 실제로 측정해서 상태에 기록하세요** (committedAt - openedAt).
이 값은 나중에 제품 지표가 됩니다.

### 3. 재생 + 청산 (EXECUTED)
계획을 저장하면 차트가 봉 하나씩 앞으로 갑니다 (기본 1초당 1봉, 배속 조절 가능).

- 손절가에 닿으면 자동 청산되고 "계획대로 손절됐습니다" 표시
- 목표가에 닿으면 자동 청산
- 사용자가 중간에 "지금 판다" 버튼을 눌러도 됨
  → 이 경우 `adhered: false`로 기록 (계획을 어긴 것)
- 최대 30봉까지 진행 후 강제 청산

**청산 직후 손익을 절대 표시하지 마세요.** 차트도 청산 시점에서 멈춥니다.

### 4. 자기 채점 화면 (GRADED로 가는 관문) ★★

전체 화면을 덮는 채점 화면을 띄웁니다. 여기가 제품의 심장입니다.

상단에 큰 글씨:
  "결과를 보기 전에, 당신의 판단부터 채점합니다."
그 아래 작게:
  "돈을 벌었는지 먼저 보면, 사람은 반드시 '벌었으니 잘한 거지'라고 채점합니다.
   그래서 순서를 바꿉니다."

화면에 보여줄 것 (손익 제외):
- 내가 저장했던 계획 3줄 (셋업 / 손절가 / 목표)
- 실제로 어떻게 청산됐는지 (손절 도달 / 목표 도달 / 직접 청산 / 시간 초과)
- 진입 시점부터 청산까지의 차트 (단, **가격 축 라벨과 손익 숫자는 숨김**)

**채점 등급은 사용자가 4개 중 고르는 게 아니라, 실행 기록으로 시스템이 먼저 판정합니다**
(2026년 개정 — 최초 설계는 A~D를 사용자가 직접 골랐으나, 손절이 항상 자동이라 D가 나올 수
없고 A·C도 이미 정해진 사실을 다시 묻는 꼴이었습니다. 그래서 "지켰는지"는 기록으로 판정하고,
사용자에게는 "판단이 좋았는지"만 묻도록 바꿨습니다):

  계획대로 청산(손절/목표/시간초과) = 준수   → 최고 등급 A까지 가능
  직접 청산으로 계획보다 먼저 나감 = 이탈    → C 고정
  손절가를 내리거나 원래 손절 아래까지 버팀 = 손절 무시 → D 고정

준수로 판정된 경우에만, 아래 두 질문(예/아니오, 큰 카드 버튼)으로 A와 B를 가릅니다:

  1. 산 이유가 된 셋업 모양이 분명했나요?
  2. 손절가를 차트 근거로 정했나요?

둘 다 "예"면 A, 하나라도 "아니오"면 B입니다. 서버는 이 판정을 클라이언트와 동일한 로직으로
다시 계산해서, 실행 기록이 허락하는 것보다 좋은 등급으로 스스로를 채점하는 것을 거부합니다
(더 나쁘게 매기는 것은 허용합니다).

### 5. 결과 공개 (REVEALED)
채점을 마쳐야 비로소:
- 이번 판단의 R 결과 (+2.0R 등, 큰 숫자)
- 2×2 매트릭스에서 내 위치 표시:

           좋은 결과   나쁜 결과
  좋은판단   정상        정상
  나쁜판단  ⚠위험       정상

  "나쁜 판단 × 좋은 결과" 칸에 들어가면 빨간 경고와 함께:
  "이번엔 운이 좋았습니다. 이 방식을 반복하면 결국 잃습니다."

## 결과 잠금 구현 규칙 (반드시 지킬 것)

- 상태가 GRADED 미만이면 스토어의 `rep.result`는 **undefined여야 합니다.**
  값을 넣어두고 UI에서만 숨기는 방식은 금지입니다.
- 결과 계산은 채점 완료 시점에 실행합니다.
- `lib/rep/machine.ts`에 `assertRevealable(rep)` 가드를 만들고,
  결과를 읽는 모든 곳에서 이 가드를 통과하게 하세요.
- Vitest로 "GRADED 이전에 result에 접근하면 throw" 테스트를 작성하세요.

## 만들지 말 것
- 계획 수정 기능
- "결과 미리보기", "이번엔 그냥 넘어가기" 같은 우회로
- 소셜 공유

## 완료 기준
1. 계획 저장 → 재생 → 청산 → 채점 → 공개 순서로만 진행된다
2. 채점 전에 어떤 화면에서도 손익 숫자가 보이지 않는다
3. React DevTools로 Zustand 스토어를 열어도 채점 전엔 result가 없다
4. Rep 카드 작성이 실제로 30초 안에 끝난다 (직접 재보세요)
```

---

# [STAGE 4] 지표 엔진 + 즉시 피드백

```
4단계를 구현하세요. 연습이 "쌓인다"는 감각을 만드는 단계입니다.

## 1. `lib/metrics/` — 계산 로직 (순수 함수 + 테스트 필수)

  rMultiple(entry, exit, stop, side): number
    = (exit - entry) / |entry - stop| * (side === 'long' ? 1 : -1)

  expectancy(reps): number          // R의 평균
  sigmaR(reps): number              // R의 표준편차
  requiredSample(reps): number      // n* = (2σ / E[R])²  , E[R]<=0이면 Infinity
  adherenceRate(reps): number       // adhered === true 비율
  setupAccuracy(reps): number       // 사용자가 고른 셋업 == setupLabel 비율
  gradeDistribution(reps)           // A~D 개수
  luckyBadTrades(reps): number      // 등급 C·D 이면서 R > 0 인 비율 ★

각 함수마다 Vitest 테스트를 쓰세요. 특히 requiredSample은
표본이 3개 미만이거나 σ가 0일 때 안전하게 처리해야 합니다.

## 2. 상시 표시줄 (모든 화면 상단 고정)

한 줄에 이렇게 나옵니다. 이게 이 제품에서 가장 중요한 UI 한 줄입니다.

  현재 63회  ·  결론까지 81회 남음  ·  평균 +0.18R  ·  계획 지킴 92%

"결론까지 N회 남음"에 물음표 아이콘을 달고, 누르면 이 설명을 띄우세요:

  "지금까지의 성적이 실력인지 운인지 판단하려면 이만큼 더 필요합니다.
   대부분의 사람이 30~50번 해보고 '이 방법 안 되네' 하며 그만두는데,
   그 횟수로는 동전던지기와 구별이 안 됩니다."

## 3. 즉시 피드백 카드 (결과 공개 화면 하단)

Rep 1회가 끝날 때마다 아래 3줄이 **반드시** 나옵니다. 로딩 없이 즉시.

  [1] 이번 판정
      "계획대로 손절했습니다. 손실이지만 잘한 거래입니다."

  [2] 이번으로 달라진 것
      "계획 지킴 비율 91% → 92%  ·  결론까지 82회 → 81회"
      변한 숫자는 화살표와 함께, 좋아졌으면 --good, 나빠졌으면 --warn

  [3] 다음 한 가지
      규칙 기반으로 딱 하나만 제시합니다 (여러 개 주면 아무것도 안 지킵니다)

피드백 규칙은 `lib/feedback/rules.ts`에 우선순위 배열로 만드세요.
첫 번째로 조건이 맞는 것 하나만 반환합니다:

  1. 등급 D가 나옴
     → "손실 한도를 지키는 것이 지금 가장 중요합니다. 다음 연습은 손절가를
        먼저 정하고 시작하세요."
  2. 최근 10회 중 계획 어김이 3회 이상
     → "최근에 계획을 자주 바꾸고 있습니다. 실력보다 습관 문제일 가능성이 큽니다."
  3. 등급 C·D인데 수익이 남 (luckyBad)
     → "이번엔 운이 좋았습니다. 같은 방식을 10번 하면 대부분 잃습니다."
  4. 셋업 판별이 최근 10회 중 5회 이하로 맞음
     → "차트 모양을 구분하는 연습이 더 필요합니다. 눌림목과 돌파의 차이를 다시 보세요."
  5. 준수율 95% 이상이고 표본이 목표의 절반 이상
     → "실행은 안정적입니다. 이제 횟수만 채우면 됩니다."
  6. (기본) "잘 진행되고 있습니다. 다음 연습으로 넘어가세요."

**LLM을 호출해서 이 즉시 피드백을 만들지 마세요.** 느리고, 매번 달라지고, 틀립니다.
(2026년 추가 — 이 원칙은 이 "즉시 피드백"에만 적용됩니다. 결과 공개 화면에는 별도로,
사용자가 직접 눌러야만 호출되는 선택적 "AI 코치 조언" 기능이 나중에 추가됐습니다.
아래 "출시 이후 추가된 기능" 참고.)

## 4. `/journal` 기록 화면
- Rep 목록 테이블: 날짜 / 셋업 / R / 등급 / 계획 지킴
- 행을 누르면 그 때의 차트와 계획을 다시 봄
- 필터: 셋업별, 등급별
- CSV 내보내기 버튼

## 5. `/progress` 진척 화면
- 상단: 숫자 4개 카드 (평균 R / 남은 횟수 / 계획 지킴 / 판별 정확도)
- R 누적 곡선 (recharts, 선 하나)
- 등급 분포 막대
- 2×2 매트릭스 누적 (⚠칸 비율을 크게 강조)

## 만들지 말 것
- 승률을 큰 숫자로 표시 (작게, 부가 정보로만)
- 수익금(원화) 표시
- 다른 사용자와 비교

## 완료 기준
1. 20회 연습 후 모든 숫자가 맞게 계산된다 (수기로 검산해보세요)
2. 결과 공개 후 피드백 3줄이 즉시(로딩 없이) 나온다
3. 표본이 5개 미만일 때 "아직 판단하기 이릅니다"로 표시된다
```

---

# [STAGE 5] 온보딩 + 3단계 게이트

```
5단계를 구현하세요. 초보자가 처음 5분을 넘기게 만드는 단계입니다.

## 1. 온보딩 (/onboarding) — 5분, 5화면

주식을 전혀 모르는 사람 기준으로 씁니다. 각 화면은 한 가지만 다룹니다.

  [1/5] 왜 이걸 하나
      "혼자 주식을 하면 실력이 늘지 않는 이유가 있습니다."
      3장의 카드: 결과가 거짓말을 한다 / 피드백이 늦다 / 기억이 조작된다
      → "그래서 이 서비스는 순서를 바꿉니다"

  [2/5] 얼마를 걸 건가
      계좌 금액 입력 (기본값 1,000만원)
      → 자동 계산: "한 번에 최대 10만원까지만 잃겠습니다 (1%)"
      → "이 10만원이 앞으로 계속 나올 '1R'입니다"
      슬라이더로 0.5%~2% 조정 가능, 2% 넘기면 경고

  [3/5] 어떤 모양을 살 건가
      셋업 3종 중 하나 선택. **직접 정의하게 하지 마세요** — 초보자는 못 합니다.
      각 선택지에 실제 차트 그림과 한 줄 설명:
        · 눌림목 — 오르던 주식이 잠깐 쉴 때 산다
        · 돌파 — 오랫동안 못 넘던 가격을 뚫을 때 산다
        · 둘 다 — 어려우니 나중에 고르세요 (2단계 게이트 이후 해금)

  [4/5] 손 잡고 한 번 해보기 ★
      실제 연습 1회를 가이드와 함께 진행합니다.
      각 단계마다 화면에 말풍선이 뜨고, 그 단계를 끝내야 다음이 열립니다.
        "① 차트를 보세요. 오른쪽은 아직 가려져 있습니다."
        "② 여기서 살지 결정하세요."
        "③ 얼마까지 내려가면 틀린 건지 정하세요. 이게 손절가입니다."
        "④ 이제 결과를 보기 전에, 당신 판단을 먼저 채점합니다." ← 여기가 클라이맥스
        "⑤ 이제 결과입니다."
      가이드 Rep은 통계에 포함하지 않습니다.

  [5/5] 앞으로의 계획
      3단계 게이트 지도를 보여주고 "1단계: 300회" 목표를 제시합니다.
      "하루 10번씩 하면 한 달입니다"처럼 체감 가능한 단위로 환산해주세요.

## 2. 게이트 시스템 (`lib/gate/`)

  G1 실행    누적 300회   준수율 ≥95%  AND  A등급 비율 ≥70%
  G2 판별    누적 600회   셋업 판별 ≥70%  AND  평균 ≥+0.2R  AND  n ≥ n*
  G3 전환    +100회       (실계좌 연동 필요 — 지금은 안내만)

(2026년 개정 — G1의 두 번째 조건은 원래 "A·B등급 ≥80%"였습니다. 그런데 준수 여부를
시스템이 실행 기록으로 판정하도록 바꾸면서, A·B(=계획 준수)는 이미 "준수율 ≥95%" 조건과
같은 것을 두 번 보는 셈이 됐습니다. 그래서 판단 근거까지 분명했던 거래(A)가 대부분인지를
보도록 바꿨습니다.)

규칙:
- 게이트를 건너뛸 수 없습니다. 다음 게이트 조건은 미리 볼 수 있습니다.
- **강등**: 최근 50회 준수율이 85% 미만으로 2주 지속되면 이전 게이트로.
  강등 시에는 "실패"가 아니라 "재조정"으로 표현하세요.
- G2의 `n ≥ n*` 조건이 핵심입니다. 성적이 좋아도 표본이 모자라면 통과 못 합니다.

## 3. 게이트 진척 UI

미통과 시 **어떤 조건이 얼마나 부족한지** 항목별 진척 막대로 보여줍니다.
이게 이탈 방어의 핵심입니다. "아직 통과 못 함"만 띄우면 사람이 나갑니다.

  누적 횟수      ████████░░  247 / 300
  계획 지킴      █████████░  92% / 95%     ← 3%p 부족
  A 등급 비율    ██████████  73% / 70%  ✓

가장 부족한 항목 하나에 "지금 여기에 집중하세요" 라벨을 답니다.

## 만들지 말 것
- 뱃지, 포인트, 콤보, 스트릭 같은 게임 요소
  (연습의 목적이 점수 모으기로 바뀌면 제품이 망합니다)
- 유료 결제

## 완료 기준
1. 주식을 모르는 사람이 온보딩 5화면을 혼자 통과할 수 있다
   (실제로 옆사람에게 시켜보세요)
2. 게이트 조건을 채우면 자동으로 승급 화면이 뜬다
3. 강등 조건을 인위적으로 만들면 실제로 강등된다
```

---

# [STAGE 6] 서버 이전 + 결과 잠금 진짜로 만들기

```
6단계를 구현하세요. 지금까지의 결과 잠금은 클라이언트에만 있어서
마음먹으면 우회됩니다. 이걸 서버에서 강제합니다.

## 1. Supabase 연동
- 이메일 매직링크 로그인 (비밀번호 없음)
- 테이블: users / setups / reps / gate_progress
- RLS: 사용자는 자기 데이터만 읽고 씁니다

## 2. reps 테이블 스키마 핵심

  id, user_id, setup_id, scenario_seed,
  state              -- 'COMMITTED'|'EXECUTED'|'GRADED'|'REVEALED'
  committed_at, commit_hash,     -- 계획 저장 시점 + 필드 해시
  plan_setup, plan_stop, plan_target_r,   -- 저장 후 UPDATE 금지
  exit_price, exit_reason, adhered, decision_grade,
  input_seconds      -- 카드 작성에 걸린 시간

DB 트리거로 `state`가 COMMITTED 이상이면 plan_* 컬럼 UPDATE를 거부하세요.

## 3. 결과 잠금의 서버 강제 ★

`GET /api/reps/[id]` 라우트에서:

  if (rep.state !== 'GRADED' && rep.state !== 'REVEALED') {
    // 결과 관련 필드를 응답 객체에서 삭제한다
    delete payload.exit_price
    delete payload.r_result
    delete payload.future_candles
  }

**응답 페이로드에서 지우는 것이 핵심입니다.** 보내놓고 프론트에서 숨기면
브라우저 네트워크 탭만 열어도 뚫립니다.

`POST /api/reps/[id]/grade`가 성공해야 결과 조회가 열립니다.

## 4. 마이그레이션
localStorage에 있던 기존 연습 기록을 첫 로그인 시 서버로 올립니다.
실패해도 로컬 데이터를 지우지 마세요.

## 완료 기준
1. 브라우저 네트워크 탭에서 채점 전 응답을 열어도 결과 필드가 없다
2. Postman으로 plan_stop을 UPDATE 시도하면 DB가 거부한다
3. 로그아웃 후 다시 로그인해도 기록이 남아 있다
```

---

# [STAGE 7] 마감 — UX 디테일과 접근성

```
7단계를 구현하세요. 여기서 "만든 것"과 "쓸 만한 것"이 갈립니다.

## 1. 속도 (30초 예산 지키기)
- 진입/청산/채점 전부 키보드로 가능하게:
  B=산다, S=지나간다, 1/2/3=셋업 선택, Enter=저장, A/B/C/D=채점
- 화면 어딘가에 단축키 힌트를 항상 작게 표시
- 손절가 입력에 프리셋 칩: -2% / -3% / -5%
- 모바일: 주요 버튼을 화면 하단 1/3(엄지 도달 범위)에 배치

## 2. 빈 상태 (처음 온 사람이 보는 화면)
모든 목록·차트에 빈 상태를 만드세요. "데이터가 없습니다"는 금지입니다.
  기록 화면: "아직 연습 기록이 없습니다. 첫 연습은 2분이면 끝납니다. [시작하기]"
  진척 화면: "5회 이상 연습하면 여기에 통계가 나옵니다. (지금 2회)"

## 3. 실수 방지
- 손절가를 현재가보다 높게 넣으면 저장 버튼 비활성 + 이유 표시
- 계좌의 5%를 넘는 손절폭이면 경고 다이얼로그
- 재생 중 새로고침하면 진행 상태를 복구
- 채점 화면에서 뒤로가기를 눌러도 결과가 안 열림

## 4. 접근성
- 모든 인터랙티브 요소에 포커스 링
- 차트에 대체 텍스트 요약 ("최근 20봉 상승 후 3봉 하락, 20일선 근접")
- 색만으로 정보를 전달하지 않기 (상승/하락에 부호나 아이콘 병기)
- 명암비 4.5:1 이상

## 5. 고지 문구 (하단 고정)
"REPS는 가상 데이터를 사용하는 훈련용 서비스입니다. 실제 주문이 실행되지 않으며,
특정 종목의 매수·매도를 권유하지 않습니다. 연습 성과는 실제 투자 성과를
보장하지 않습니다."

## 6. 성능
- 시나리오 생성을 Web Worker로 옮겨 메인 스레드를 막지 않게
- 차트는 동적 import로 코드 스플리팅
- 다음 시나리오를 미리 생성해두기 (연습 사이 대기 0초)

## 완료 기준
1. 마우스 없이 키보드만으로 연습 1회를 끝낼 수 있다
2. Lighthouse 접근성 90점 이상
3. 연습과 연습 사이에 로딩이 보이지 않는다
4. 연습 10회 연속으로 해도 렉이 없다
```

---

# 출시 이후 추가된 기능 (2026년, 스테이지 이후)

7단계까지는 위 프롬프트대로 만들어졌습니다. 그 이후 실제로 써보면서 추가되거나 바뀐
내용은 프롬프트가 아니라 사실 기록으로 남깁니다 — 코드가 기준입니다.

- **게스트 모드**: 로그인 없이도 최대 5회까지 연습할 수 있습니다. 기록은 이 브라우저에만
  저장되고, 로그인하면 계정으로 옮겨집니다(서버가 R·준수 여부·등급을 다시 계산해서 검증).
- **온보딩 설정이 실제 연습에 반영**: 계획 카드에 "1R = OO원, 이만큼 사려면 OO주" 수량
  계산이 나오고, 1단계(G1)에서는 온보딩에서 고른 셋업과 셋업 없음 차트만(약 7:3) 나옵니다.
- **설정 화면(`/settings`)**: 온보딩 이후에도 계좌 금액·위험 비율·셋업 선호를 바꿀 수
  있습니다. 게스트도 쓸 수 있고, 이 경우 이 브라우저에만 저장됩니다.
- **결과 공개 화면 보강**: 청산 이후 차트, 이 연습의 정답 셋업, 계획을 직접 청산해서
  바꿨을 경우 "계획대로였다면 어땠을지" 비교를 함께 보여줍니다.
- **진행 상황 체감**: 상단에 하루 목표(10회) 진행률, 진척 화면에 이번 주 vs 지난주 비교와
  최근 속도 기준 목표 도달 예상일이 추가됐습니다. 뱃지·포인트·스트릭 없음(원칙 그대로).
- **게이트 판정을 서버로 이전**: 승급·강등은 클라이언트가 아니라 서버가 서버에 저장된
  기록만으로 판정합니다. 기기를 바꿔도 단계가 달라지지 않습니다.
- **AI 코치 조언 (선택 기능)**: 결과 공개 후 "AI 코치에게 물어보기" 버튼을 누르면 Anthropic
  Claude API가 그 판단의 실제 사실(셋업 정답 여부·등급·R)만 근거로 짧은 코칭 문장을
  만들어줍니다. 매 판단마다 자동으로 뜨는 위의 즉시 피드백과는 다른, 별도의 opt-in
  기능입니다. 로그인한 사용자만 쓸 수 있습니다(게스트는 서버에 기록이 없어서).

---

# [QA] 단계별 자가 점검 프롬프트

각 단계가 끝날 때마다 붙여넣으세요.

```
방금 만든 코드를 아래 항목으로 점검하고, 위반한 것을 전부 찾아서 고치세요.
"문제 없습니다"라고 하기 전에 실제로 파일을 열어 확인하세요.

## 원칙 위반 점검
1. 채점(GRADED) 전에 결과 데이터에 접근할 수 있는 경로가 하나라도 있는가?
   - 스토어, props, API 응답, URL 쿼리, localStorage 전부 확인
2. Rep 카드 입력 필드가 3개를 넘는가?
3. 계산 로직이 UI 컴포넌트 안에 들어가 있는가? (lib/ 밖에 있으면 위반)
4. 수익금(원화)이나 수익률(%)이 R 대신 표시되는 곳이 있는가?
5. 순위·리더보드·뱃지·포인트가 생겼는가?

## 초보자 관점 점검
6. 화면에 나오는 전문 용어 중 <Term>으로 안 감싼 것이 있는가?
   전체 텍스트를 훑어서 목록을 만드세요.
7. 숫자만 덩그러니 나오고 "이게 무슨 뜻인지" 설명이 없는 곳이 있는가?
8. 사용자가 다음에 뭘 해야 할지 모르게 되는 화면이 있는가?

## UI 점검
9. 색상을 CSS 변수 대신 하드코딩한 곳이 있는가?
10. 숫자에 tabular-nums가 빠진 곳이 있는가?
11. 375px 폭에서 가로 스크롤이 생기는가?
12. 상승=빨강 / 하락=파랑이 지켜졌는가? (미국식으로 되어 있으면 위반)

각 항목에 대해 "확인함 / 위반 발견 → 수정함"으로 답하고,
위반이 있었다면 어느 파일 몇 번째 줄이었는지 밝히세요.
```

---

# [DEBUG] 막혔을 때

```
지금 이 문제가 있습니다: [문제 설명]

고치기 전에 아래를 먼저 하세요:
1. 원인이라고 생각하는 가설을 3개 쓰세요
2. 각 가설을 어떻게 확인할지 쓰세요
3. 실제로 확인한 결과를 쓰세요
4. 그 다음에 고치세요

추측으로 코드를 여기저기 바꾸지 마세요.
그리고 이 문제를 고치면서 MASTER의 5개 원칙을 깨지 않는지 확인하세요.
```

---

## 단계 요약

| 단계 | 만드는 것 | 이게 없으면 |
|---|---|---|
| 1 | 골격 · 디자인 토큰 · 용어 툴팁 | 초보자가 첫 화면에서 나감 |
| 2 | 차트 생성기 · 블라인드 차트 | 연습할 대상이 없음 |
| **3** | **Rep Card · 결과 잠금** | **그냥 또 하나의 모의투자 앱** |
| 4 | 지표 · 즉시 피드백 | 연습이 쌓인다는 감각이 없음 |
| 5 | 온보딩 · 3단계 게이트 | 언제 실전에 나갈지 모름 |
| 6 | 서버 강제 · 저장 | 잠금이 우회됨 |
| 7 | 속도 · 접근성 · 마감 | 만들었지만 안 씀 |

**3단계가 전부입니다.** 1·2단계는 3단계를 위한 준비이고, 4~7단계는 3단계를 쓸 만하게 만드는 작업입니다. 시간이 없으면 3단계까지만 만들어도 제품은 성립합니다.

7단계 이후 실제 출시하며 바뀌거나 추가된 내용은 위 "출시 이후 추가된 기능"과, 코드베이스의 `CLAUDE.md`를 참고하세요.
